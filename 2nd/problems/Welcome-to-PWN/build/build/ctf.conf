service welcome_pwn
{
    type = UNLISTED
    protocol = tcp
    socket_type = stream
    port = 60000
    wait = no
    disable = no
    user = pwn
    server = /bin/sh
    server_args = /home/pwn/start.sh
}
